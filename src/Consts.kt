class Consts {
    companion object {
        const val RED = "RED"
        const val BLUE = "BLUE"
        fun addValues(a:Int,b:Int ):Int {return a.plus(b)}
        fun subtractValues(a:Int,b:Int ):Int {return a.minus(b)}
        fun divideValues(a:Int,b:Int ):Int {return a.div(b)}
        fun multiplyValues(a:Int,b:Int ):Int {return a.times(b)}
    }
}